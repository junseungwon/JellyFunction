If you're using Forward+ as your rendering path, you need to use the shaders inside this .unitypackage instead, because Unity breaks them and you'll see some flickering going on with the normal shaders.